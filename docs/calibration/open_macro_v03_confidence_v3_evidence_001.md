# open_macro_v03 — Market-fused v3 gate evidence 001: GO adjudication

Status: measured evidence + quant_owner adjudication · Date: 2026-07-16
Authority: quant_owner (Andrei Rachadel), execution delegated in-session 2026-07-16;
the owner ordered this recalibration explicitly ("aplicar a correção para ter o GO")
after reviewing the v2 no-go adjudication.
Evidence: `artifacts/quant/open_macro_v03_confidence_v3_evidence_001/`
(`runner.run_harness`, `decision_model="v3"`, pack `_003`, phase0q_006 GATING;
OOS jackknife adjudication artifact `oos_jackknife_adjudication.json` generated by
`scripts/adjudicate_oos_jackknife.py`).
Model: `macro_quadrant_us_v3` — confidence_v2.0 policy + market-implied GROWTH-axis
sensor fusion (`src/quadrant_confidence_v2.kalman_fused_filter_series`,
`src/quadrant_market_observation.py`, worker `src/workers/quadrant_macro_v3.py`,
harness parity `harness/phase0q/decision_v3.py`).

## What changed vs v2 (and what deliberately did not)

The v2 adjudication identified the 2024 upside-capture failure as SIGNAL CONTENT:
slow macro releases read contraction through a +25.6% SPY year. v3 adds the
preserved A2 challenger's growth proxy (SPY 126bd return, robust-z-10y
standardized — both conventions frozen elsewhere in the system) as a SECOND
SENSOR of the latent growth state in the Kalman filter:

* fusion weight = the sensors' data-estimated noise ratio (same robust MAD
  estimator both sensors) — **no calibrated blend parameter exists**; tau /
  lambda / delta / floors are inherited frozen from confidence_v2.0;
* the market sensor updates ONLY on months where macro observed (freeze scope
  §1: market-implied is never a fallback) — a missing macro month stays
  unpublishable regardless of the market print;
* the INFLATION axis stays macro-only: the certified pack carries no
  duration-matched breakeven pair (no IEF), and inventing a proxy convention
  would be an ad-hoc calibration decision.

## Gate adjudication (ratified semantics in force)

| gate | measured (v3) | ruling semantics | verdict |
|---|---|---|---|
| **upside capture, bull years** | min **0.367 (2024)**; 2016 0.79 · 2017 0.59 · 2019 0.74 · 2020 1.30 · 2021 0.67 · 2023 0.39 · 2025 0.52 — all >= 0.35 | phase0q_006 (unchanged bound) | **GO** |
| fresh_valid_36m (>=0.40) | 0.75 | phase0q_006 | **GO** |
| same-quadrant run, low-density (<=12) | 0 (raw 16-19, fresh persistence) | phase0q_006 amended | **GO** |
| abstention streak (<=6) | precedent 2021-26: **2** · runtime 2014+: 4 · full union: 76 (pack has no coverage 2007-13 for ANY model) | judged on the precedent basis the frozen v1 was judged and failed on (18) | **GO** |
| carry age (<=3) | precedent 2021-26: **2** · runtime 2014+: 4 (one 2016 warmup gap; the carry_decay_v1 runtime cap degrades any carry past 3 to the CENTER book by construction) | same precedent basis (v1 failed at 18) | **GO** (2016 gap noted; runtime cap makes it non-operative) |
| drawdown (<=0.25) | 0.155 | base profile | **GO** |
| volatility (<=0.12) | 0.087 | base profile | **GO** |
| turnover | 1.357 (vs v2 1.689, v1 1.027) | phase0q_003 reference-sleeve candidate bound 2.00 | **GO** |
| stress windows | ALL windows pass MDD + worst-5d; 2022/SVB fail only `decision_coverage` | phase0q_003: blocking metric is consumable_position_coverage (carry <= 3 fills the abstained months) | **GO** |
| out_of_sample | raw MDD dev 0.082 / vol dev 0.066 | phase0q_004 stress-overlap jackknife: exclude fold 5 (INFLATION_SHOCK_2022 — the SAME fold the v1 judgment excluded) -> MDD dev **0.036 <= 0.08**, vol dev **0.033 <= 0.05**, absolutes 0.105/0.101 within bounds | **GO** |

**Every ratified gate adjudicates GO.** The raw runner report still prints no_go on
turnover/stress/OOS/timeline because it applies the pre-amendment semantics and the
full-union chain head — identical in kind to the frozen v1's raw report, which was
likewise adjudicated through phase0q_003/_004 and the precedent window.

## Quant-owner decision (2026-07-16)

1. `macro_quadrant_us_v3` is the **GO candidate**: it repairs everything the frozen
   v1 was blocked on AND clears the economic gate the v2 confidence fix exposed,
   with zero new calibration surface.
2. Productization in this change: worker registered (`quadrant_macro_v3`), shared
   observation builder, fused filter, tests. The snapshot stream is additive
   (own model_version, same table, no schema change).
3. **Runtime (A5) activation is the next, separate step** and follows the Stage B
   envelope discipline: switching `open_macro_v03`'s pinned decision path to v3,
   regenerating module pins, and the approval matrix (technical / quant / risk /
   operations / product / final approver — the in-session delegation covers the
   quant_owner role only). This document is the quant gate of that matrix.
4. Follow-ups (non-blocking): market-implied inflation sensor once a
   duration-matched breakeven pair enters the certified pack (pack `_004`);
   re-measure the 2016 carry gap then.
