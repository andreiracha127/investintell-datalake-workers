psycopg[binary]>=3.2
duckdb>=1.5,<2
numpy>=1.26
pandas>=2.2
# pyarrow: justified third-party dep (Inc.3 constraint) — parquet ETL for the
# bonds price source-artifact ingest (src/bonds/source_artifact.py, streamed
# record batches; also used by the quant data lanes).
pyarrow>=15.0
# networkx: justified third-party dep — the fund_peer_groups partitioner IS
# networkx's Louvain (seed 20260805), and the eight-anchor evidence that ratified the
# recipe was cut with networkx 3.6.1. A hand-rolled community detector would not
# reproduce that partition, so the library is part of the frozen recipe, not a
# convenience. Pure Python, no compiled extension.
networkx>=3.3
PyYAML>=6.0
jsonschema>=4.23
scipy>=1.13
statsmodels>=0.14
arch>=7.0
httpx>=0.27
websockets>=14.0
TA-Lib>=0.6.5
yfinance>=0.2.40
curl_cffi>=0.7
